spring:
  datasource:
    url: "jdbc:mysql://localhost/warming_up" # ???? ??? ?????? ??
    username: "root"
    password: "root1234"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      # create -> ?? ???? ??? ?? ? ?? ??
      # create-drop -> ???? ??? ? ???? ?? ??
      # validate -> ??? ???? ???? ??
      # update -> ??? ???? ?? ??? ??
      # none -> ??? ?? X
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
